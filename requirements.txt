# DoRA — Python dependencies (PyTorch 2.x, CUDA 12.x)
#
# Quick-start (conda):
#   conda create -n dora python=3.10
#   conda activate dora
#   conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
#   pip install -r requirements.txt
#
# Alternatively (pip-only, adjust CUDA wheel as needed):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

# Core deep-learning
# torch>=2.3.0          # installed via conda/wheel above
# torchvision>=0.18.0   # installed via conda/wheel above

# Video decoding
decord>=0.6.0

# Tensor utilities
einops>=0.7.0

# Nearest-neighbor search (KNN evaluation)
# GPU build: install via conda:  conda install -c pytorch faiss-gpu
# CPU fallback: faiss-cpu>=1.8.0
faiss-cpu>=1.8.0

# Image handling
Pillow>=10.0.0

# Scientific computing
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Logging / visualization
tensorboard>=2.14.0
tqdm>=4.66.0

# Misc utilities used by the codebase
six>=1.16.0
lmdb>=1.4.1
pyarrow>=14.0.0
munkres>=1.1.4
